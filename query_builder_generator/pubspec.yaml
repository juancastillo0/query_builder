name: query_builder_generator
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.14.0 <4.0.0"

dependencies:
  build: ^2.0.0
  source_gen: ^1.0.0
  analyzer: ">=5.0.0 <7.0.0"
  query_builder:
    git:
      url: https://github.com/juancastillo0/query_builder

dev_dependencies:
  build_runner: ^2.0.0
  build_config: ^1.0.0
  test: ^1.16.0
